// ----------------------------------------------------------------------------
//
//  Welcome to Baml! To use this generated code, please run the following:
//
//  $ go get github.com/boundaryml/baml
//
// ----------------------------------------------------------------------------

// This file was generated by BAML: please do not edit it. Instead, edit the
// BAML files and re-generate this code using: baml-cli generate
// You can install baml-cli with:
//  $ go install github.com/boundaryml/baml/baml-cli

package baml_client

var file_map = map[string]string{

	"generators.baml": "// Generator for the de-BAML Slice 7.2c-3 ISOLATED operator serve fixture `ne`.\n//\n// WHY THIS PROJECT EXISTS AT ALL. Slice 7.2c-3 widens the admitted static predicate\n// from `this > I` to the six direct comparisons, and the live proof has to drive a\n// REAL generated /call route per operator. One BAML project cannot declare\n// `StaticCheckedAnswer` six times, and the 7.2c scope forbids renaming the classes to\n// make the variants coexist — a renamed class is a different family with no stock\n// capture behind it, and the main staticserve_fixture deliberately keeps a\n// `StaticGtePredicateAnswer` sibling DECLINED so the name pin means something.\n//\n// So each operator gets an ISOLATED project that declares the two PINNED names ONCE\n// with its own predicate. `>` is not here: it is the main staticserve_fixture's, which\n// is unchanged.\n//\n// It is a TEST-ONLY artifact — excluded from the customer/container source embed via\n// .embedignore — and never ships in a production build. Its own loopback PORT (17659)\n// keeps it addressable independently of the other fixtures, so two operator packages\n// can run concurrently and a request is attributable to the project that received it.\n//\n// Pinned to 0.223.0 to match the stock github.com/boundaryml/baml the serve tests link.\n// Regenerate via scripts/regen-staticserve-fixture.sh.\ngenerator static_serve_op_fixture_ne {\n  output_type \"go\"\n  output_dir \"../\"\n  version \"0.223.0\"\n  default_client_mode async\n  client_package_name \"github.com/invakid404/baml-rest/internal/nativeprompt/testdata/staticserve_op_fixtures/ne\"\n}\n",
	"main.baml":       "// The ISOLATED != operator project of de-BAML Slice 7.2c-3.\n//\n// It declares the two PRODUCTION-PINNED class names exactly ONCE each, with this\n// project's own predicate. Nothing else: no siblings, no extra classes, no second\n// definition of either name — TestOperatorFixtureProjectsAreIsolatedAndPinned parses\n// this file and fails if any of that changes.\n\nclient<llm> StaticOracleClient {\n  provider openai\n  options {\n    model \"fake-static-oracle-model\"\n    api_key \"fake-static-oracle-key\"\n    base_url \"http://127.0.0.1:17659/v1\"\n  }\n}\n\nclass StaticCheckedAnswer {\n  answer string\n  confidence int @check(positive, {{ this != 0 }})\n}\n\nclass StaticAssertAnswer {\n  answer string\n  confidence int @assert(positive, {{ this != 0 }})\n}\n\nfunction StaticCheckedConfidence(topic: string) -> StaticCheckedAnswer {\n  client StaticOracleClient\n  prompt #\"\n    Topic: {{ topic }}\n    {{ ctx.output_format }}\n  \"#\n}\n\nfunction StaticAssertConfidence(topic: string) -> StaticAssertAnswer {\n  client StaticOracleClient\n  prompt #\"\n    Topic: {{ topic }}\n    {{ ctx.output_format }}\n  \"#\n}\n",
}

func getBamlFiles() map[string]string {
	return file_map
}
