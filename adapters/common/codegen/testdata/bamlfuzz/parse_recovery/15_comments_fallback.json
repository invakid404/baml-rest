{
  "name": "comments_fallback",
  "description": "Inline block comment inside the object. BAML's JSONish parser drops the comment and recovers the record. Phase 7C: a string-aware stripJSONComments pre-pass drops /* */ and // comments outside strings before extraction, so the native parser now CLAIMS this byte-exact (native disposition: claimed) — no BAML fallback. want captured from BAML final parse.",
  "preserve_schema_order": true,
  "schema": {
    "classes": [
      {
        "name": "Root",
        "properties": [
          {"name": "name", "type": {"kind": "string"}},
          {"name": "age", "type": {"kind": "int"}}
        ]
      }
    ],
    "root_class": "Root"
  },
  "raw": "{\"name\": \"Ada\", \"age\": 36, /* note */}",
  "want": {
    "status": "success",
    "json": {"name": "Ada", "age": 36}
  }
}
