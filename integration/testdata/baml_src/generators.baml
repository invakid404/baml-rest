// Generator configuration for test fixtures
// Note: This gets removed during the build process - only the version is used for detection
generator target {
    output_type "go"
    output_dir "../"
    version "0.214.0"
    default_client_mode async
}
